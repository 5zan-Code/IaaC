apiVersion: v1
kind: Service
metadata:
  name: worker-service
  labels:
    name: worker-service
    app: demo-worker-app

spec:
  selector:
    name: worker-app-pod
    app: demo-worker-app
  
      